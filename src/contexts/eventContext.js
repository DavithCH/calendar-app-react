import { createContext } from "react";

export const EventContext = createContext();

const initialState = {};
// const eventReducer = (state, action) => {
//   switch()
// }

// export const EventContextProvider = ({chidren}) => {

// }
